# sort-algorithm-samples

## 概要
本プロジェクトは、生成AIを用いてどのようなアウトプットが可能か、およびアルゴリズムの視覚的な理解を深めることを目的として作成した学習用サンプルです。
ソートアルゴリズムの特徴、メリット・デメリット、実装難易度などを視覚的に学習・比較できるWebダッシュボードです。
https://tap1ra.github.io/sort-algorithm-samples/

## 免責事項
* 本リポジトリのコードはAIによって生成されたものです。
* 学習および動作確認を目的としており、商用利用や特定の既存サイトの模倣を意図したものではありません。
* 問題がある場合は、本リポジトリを予告なく削除または修正することがあります。

## アルゴリズム

1. **Bubble Sort**（バブルソート）
2. **Selection Sort**（選択ソート）
3. **Insertion Sort**（挿入ソート）
4. **Merge Sort**（マージソート）
5. **Quick Sort**（クイックソート）
6. **Heap Sort**（ヒープソート）

## 実行方法

### セットアップ手順

1. リポジトリをクローンします
   ```bash
   git clone https://github.com/tap1ra/sort-algorithm-samples.git
   cd sort-algorithm-samples
   ```

2. 依存関係（パッケージ）をインストールします
   ```bash
   npm install
   ```

3. ローカル開発サーバーを起動します
   ```bash
   npm run dev
   ```

4. ターミナルに表示されたURL（通常は `http://localhost:5173/` ）にブラウザでアクセスすると、アプリケーションが利用可能です。
